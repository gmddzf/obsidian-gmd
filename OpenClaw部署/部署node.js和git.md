检查 Node.js 是否安装
```bash
node -v
#-v显示版本号
#node：运行 JS 的引擎（主角）
```
检查 npm 是否配套安装
```bash
npm -v
#npm：包管理器（配角，负责装库）
```
检查 Git 是否安装
```bash
git --version
#`-v` = verbose（详细输出）
#`-version` = 查看版本号
- `-v` 小写：Git 里是详细模式
- `-V` 大写：Git 里是版本号
- `--version`：所有工具通用，最安全
```